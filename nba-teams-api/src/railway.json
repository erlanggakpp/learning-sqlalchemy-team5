{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "pip install -U pipenv && pipenv install --deploypipenv install --deploy"
    },
    "deploy": {
        "startCommand": "pip install -U pipenv && pipenv install --deploypipenv install --deploy && pipenv run gunicorn --bind 0.0.0.0:808 app:app",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
